<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <!-- Lets AdhanPlaybackService's notification launch AdhanAlarmActivity over
         the lockscreen. Auto-granted below API 34; user-revocable on Android 14+
         (we degrade to a heads-up notification if revoked). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <!-- Turns the screen on when the adhan fires. -->
    <uses-permission android:name="android.permission.TURN_SCREEN_ON"/>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <application
        android:label="Zikr"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- just_audio_background / audio_service: required foreground service
             and media-button receiver. Without these, AudioService.init()
             (via JustAudioBackground.init in main.dart) throws a
             PlatformException about the Activity / FlutterEngine. -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- android_alarm_manager_plus: background alarm service + receivers.
             Required for AndroidAlarmManager scheduling (e.g. the weekly adhan
             refresh in adhan_background.dart). Without these, scheduling throws
             "Component class ... does not exist". -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Full-adhan background auto-play: an exact alarm fires
             AdhanAlarmReceiver at each prayer time, which starts
             AdhanPlaybackService (a mediaPlayback foreground service) to play
             the full adhan via MediaPlayer. AdhanBootReceiver re-arms the
             persisted alarms after a reboot / app update. -->
        <receiver
            android:name=".adhan.AdhanAlarmReceiver"
            android:exported="false" />
        <service
            android:name=".adhan.AdhanPlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <!-- The unmissable full-screen alarm. Launched by the service's
             full-screen-intent notification, so it appears over the lockscreen
             and turns the screen on even from a killed app.
             - showWhenLocked/turnScreenOn: the manifest half of the API 27+
               Activity calls, so the window manager honours them before
               onCreate runs.
             - excludeFromRecents: an alarm shouldn't linger in the task
               switcher after it's dismissed.
             - singleInstance + own taskAffinity: keeps it out of the Flutter
               MainActivity task, so dismissing the alarm never closes the app. -->
        <activity
            android:name=".adhan.AdhanAlarmActivity"
            android:exported="false"
            android:theme="@style/AdhanAlarmTheme"
            android:launchMode="singleInstance"
            android:taskAffinity=".adhan.alarm"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode" />
        <receiver
            android:name=".adhan.AdhanBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- flutter_local_notifications: receivers for scheduled / boot-time
             rescheduling, and an action receiver for tap handling. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package visibility (Android 11+) for the OEM autostart / protected-app
             managers probed by AdhanAlarmPlugin.oemAutostartIntent(). Without
             these, resolveActivity() returns null on every one of them and the
             app can't offer the whitelist deep-link that keeps background adhan
             alarms alive on Xiaomi/Oppo/Vivo/Huawei builds. -->
        <package android:name="com.miui.securitycenter"/>
        <package android:name="com.coloros.safecenter"/>
        <package android:name="com.oppo.safe"/>
        <package android:name="com.vivo.permissionmanager"/>
        <package android:name="com.iqoo.secure"/>
        <package android:name="com.huawei.systemmanager"/>
        <package android:name="com.letv.android.letvsafe"/>
        <package android:name="com.asus.mobilemanager"/>
    </queries>
</manifest>
